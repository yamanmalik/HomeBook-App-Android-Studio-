<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pacpl.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".profileActivity" />
        <activity android:name=".searchActivity" />
        <activity android:name=".occupationActivity" />
        <activity android:name=".welcome" />
        <activity android:name=".addserviceactivity" />
        <activity android:name=".removeserviceactivity" />
        <activity android:name=".editserviceactivity" />
        <activity
            android:name=".WelcomeSP"
            android:label="@string/title_activity_welcome_sp" />
        <activity android:name=".welcomeuser"></activity>
    </application>

</manifest>